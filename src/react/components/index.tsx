import { FC } from "react";
import FieldInput from "../../core/formPath/custom/FieldInput";

const ReactComponents: FC = () => (
    <>
        <FieldInput />
    </>
);

export default ReactComponents;
