import { FC } from "react";
import Panel from "../Panel";

const ReactiveOther: FC = () => (
  <Panel header={<h2>Reactive 其他内容</h2>}>
    <p>请通过单元测试查看：</p>
    <ul>
      <li>目录：./src/__test__/fishedee</li>
    </ul>
  </Panel>
);

export default ReactiveOther;
